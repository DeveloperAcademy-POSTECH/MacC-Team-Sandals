import Foundation

enum ImageSize {
    static let large = CGSize(width: 500, height: 500)
    static let medium = CGSize(width: 300, height: 300)
    static let small = CGSize(width: 100, height: 100)
}
